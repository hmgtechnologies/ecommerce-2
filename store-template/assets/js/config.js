/* ============================================================================
 * HMG StoreForge — STORE CONFIGURATION
 * ----------------------------------------------------------------------------
 * This file is AUTO-GENERATED by the StoreForge generator from client details.
 * You can also edit it by hand. Every value here controls the live store.
 * ==========================================================================*/

window.STORE_CONFIG = {
  /* ---- Store identity ---- */
  storeId: "demo-store",
  storeName: "Demo Store",
  tagline: "Quality products at honest prices.",
  about: "We are a Nigerian-owned business committed to genuine products and great service.",
  location: "Lagos, Nigeria",
  storeUrl: "https://demo-store.pages.dev",
  announcement: "🎉 Free delivery on orders above ₦50,000!",

  /* ---- Currency ---- */
  currencySymbol: "₦",

  /* ---- Contact ---- */
  whatsapp: "2348100866322",       // international format, no +
  whatsappDisplay: "+234 810 086 6322",
  phone: "2348100866322",
  phoneDisplay: "+234 810 086 6322",
  email: "owner@demostore.com",

  /* ---- Social media (leave blank to hide) ---- */
  social: {
    facebook: "",
    instagram: "",
    tiktok: "",
    youtube: "",
    whatsapp: "https://wa.me/2348100866322"
  },

  /* ---- Theme colours ---- */
  theme: {
    primary: "#0f2a4a",
    accent: "#c9a227",
    accent2: "#14857c",
    dark: "#0a1929"
  },

  /* ---- Payments (both methods supported) ---- */
  payments: {
    manual: {
      enabled: true,
      bank: "GTBank",
      accountNumber: "0123456789",
      accountName: "Demo Store Ltd"
    },
    gateway: {
      enabled: false,
      provider: "Paystack",            // or "Flutterwave"
      paymentLink: ""                  // e.g. https://paystack.com/pay/your-link
    }
  },

  /* ---- Data backend (Supabase). Leave url/anonKey blank to use products.json / fallback ---- */
  supabase: {
    url: "",                           // e.g. https://xxxx.supabase.co
    anonKey: "",                       // public anon key (safe for client)
    table: "products"
  },

  /* ======================= v2 ENTERPRISE OPTIONS ======================= */

  /* ---- Discount / coupon codes (free). Empty array = no coupons ---- */
  coupons: [
    // { code: "WELCOME10", type: "percent", value: 10, minTotal: 0 },
    // { code: "SAVE2000",  type: "fixed",   value: 2000, minTotal: 20000 }
  ],

  /* ---- Delivery zones (free). Empty array hides the zone selector ---- */
  delivery: {
    freeAbove: 50000,                  // free delivery above this subtotal (0 = never)
    zones: [
      // { name: "Lagos Mainland", fee: 1500 },
      // { name: "Lagos Island",   fee: 2500 },
      // { name: "Outside Lagos",  fee: 4000 }
    ]
  },

  /* ---- Newsletter capture (free Formspree). Get an ID at formspree.io ---- */
  newsletter: {
    formspreeId: ""                    // e.g. "xayzabcd"  (from https://formspree.io/f/xayzabcd)
  },

  /* ---- Analytics (free). Paste IDs to enable; leave blank to skip ---- */
  analytics: {
    cloudflareToken: "",               // Cloudflare Web Analytics token (privacy-friendly, no banner)
    googleId: ""                       // e.g. "G-XXXXXXX" (Google Analytics 4)
  },

  /* ======================= v3 ENTERPRISE OPTIONS ======================= */

  /* ---- Store hours → open/closed indicator ---- */
  hours: {
    enabled: false, timezone: "Africa/Lagos",
    schedule: { mon: ["08:00","20:00"], tue: ["08:00","20:00"], wed: ["08:00","20:00"],
                thu: ["08:00","20:00"], fri: ["08:00","21:00"], sat: ["09:00","21:00"], sun: ["12:00","18:00"] }
  },
  /* ---- FAQ accordion ---- */
  faq: [],                              // [{ q:"...", a:"..." }]
  /* ---- Testimonials ---- */
  testimonials: [],                     // [{ name:"...", text:"...", rating:5 }]
  /* ---- Multi-currency display (informational) ---- */
  currencies: null,                     // { base:"NGN", rates:{NGN:1,USD:0.00067}, symbols:{NGN:"₦",USD:"$"} }
  /* ---- Returns policy text ---- */
  returnsPolicy: "We accept returns within 48 hours of delivery for unused items. Contact us on WhatsApp.",

  /* ======================= v4 ENTERPRISE OPTIONS ======================= */

  /* ---- Google Sheets order logging (free Apps Script). See integrations/GOOGLE-SHEETS-SETUP.md ---- */
  orders: {
    sheetsWebAppUrl: "",               // https://script.google.com/macros/s/XXXX/exec
    alsoEmailFormspreeId: ""           // optional Formspree ID for email backup
  },
  /* ---- Loyalty points ---- */
  loyalty: {
    enabled: false,
    pointsPerNaira: 0.001              // 1 point per ₦1,000 spent
  },
  /* ---- Flash sale countdown ---- */
  flashSale: {
    enabled: false,
    title: "Flash Sale!",
    endsAt: ""                         // ISO date e.g. "2026-07-01T23:59:59"
  },
  /* ---- Social-proof "recently sold" toasts ---- */
  socialProof: { enabled: false },
  /* ---- Trust badges (leave empty for sensible defaults) ---- */
  trustBadges: [],                      // [{ icon:"🚚", title:"...", text:"..." }]
  /* ---- Language switcher (v8: English, Pidgin, Hausa, Yoruba, Igbo) ----
     enabled:true shows a dropdown. list controls which appear; default is the
     starting language. Translations live in assets/js/i18n.js. */
  languages: { enabled: true, default: "en", list: ["en", "pcm", "ha", "yo", "ig"] },

  /* ======================= v5 ENTERPRISE OPTIONS ======================= */

  /* ---- Multi-vendor MARKETPLACE mode (Jumia/Jiji-style) ----
     Set enabled:true and list vendors. Give each product a "vendor" id
     (matching a vendor.id) in products.json. Orders are split per vendor. */
  marketplace: {
    enabled: false,
    commissionPercent: 0,              // your cut per sale (informational/admin only)
    vendors: [
      // { id: "v1", name: "Bella Fashion", whatsapp: "2348011111111", location: "Lagos", rating: 4.8, logo: "" },
      // { id: "v2", name: "Tech Hub",      whatsapp: "2348022222222", location: "Abuja", rating: 4.6, logo: "" }
    ]
  },

  /* ---- Gift cards / store credit codes (free) ---- */
  giftCards: [
    // { code: "GIFT5000", value: 5000 }
  ],

  /* ---- Referral / affiliate program (free) ---- */
  referral: {
    enabled: false,
    rewardText: "Share your link — earn store credit when friends buy!"
  },

  /* ======================= v6 ENTERPRISE OPTIONS ======================= */

  /* ---- Browser/PWA notifications (free native API) ---- */
  notifications: { enabled: false },

  /* ---- Store QR code button (free) ---- */
  qr: { enabled: true },

  /* ---- Monthly sales target widget (free) ---- */
  salesTarget: { enabled: false, monthly: 0 },   // e.g. { enabled:true, monthly:500000 }

  /* ---- Dispatch / riders (for dispatch.html). Each rider: { name, phone } ---- */
  dispatch: {
    riders: [
      // { name: "Musa (Bike)", phone: "2348011111111" },
      // { name: "Grace (Van)", phone: "2348022222222" }
    ]
  },

  /* ======================= v7 ENTERPRISE OPTIONS ======================= */

  /* ---- Buyer accounts + saved addresses (free, local) ---- */
  accounts: { enabled: true },

  /* ---- Subscriptions / recurring-order reminders (free) ---- */
  subscriptions: { enabled: true },

  /* ---- Commission ledger (for ledger.html). Your cut per vendor sale ---- */
  ledger: { commissionPercent: 8 },

  /* ======================= v9 DESIGN / CUSTOMISATION ======================= */

  /* ---- DESIGN: chosen in the generator's Customization Studio ----
     font/headingFont: system | inter | poppins | montserrat | nunito | lora | playfair | spacegrotesk
     layout:  classic | grid | magazine | minimal
     hero:    split | banner | centered | none
     cardStyle: shadow | border | flat | overlay
     uiStyle:  standard | glass | neumorph | bold
     radius:   sharp | soft | rounded | pill
     buttonShape: square | rounded | pill
     density:  compact | cozy | spacious
     darkDefault: true|false  */
  design: {
    font: "system", headingFont: "system",
    layout: "classic", hero: "split", cardStyle: "shadow", uiStyle: "standard",
    radius: "soft", buttonShape: "rounded", density: "cozy", darkDefault: false
  },

  /* ---- FEATURES: turn sections on/off per client (false = hidden) ---- */
  features: {
    search: true, categories: true, wishlist: true, reviews: true, qa: true,
    testimonials: true, faq: true, vendors: true, newsletter: true,
    recentlyViewed: true, trustBadges: true, about: true, contact: true,
    darkMode: true, languages: true, loyalty: true, accounts: true, notifications: true
  },

  /* ---- Abandoned-cart EMAIL recovery (free Formspree) ---- */
  recovery: {
    enabled: false,
    formspreeId: "",                 // your Formspree form id
    delayMinutes: 60,
    coupon: "COMEBACK10",
    couponHint: "10% off"
  }
};
